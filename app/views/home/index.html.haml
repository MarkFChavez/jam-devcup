= form_for @message do |f|
  %div
    = f.label :subject
    = f.text_field :subject, class: "form-control"

  %div
    = f.label :body
    = f.text_area :body, class: "form-control"

  %div
    = f.label :send_at
    = f.datetime_field :send_at, class: "form-control"

  = f.submit
